---
- name: "Including tasks for {{ item }}"
  ansible.builtin.include_tasks: "{{ task_item }}.yml"
  loop:
    - sshd
    - env
    - git
    - prompt
    - wsl
    - asdf
    - packages
    - completion
    - systemd
  loop_control:
    loop_var: task_item
