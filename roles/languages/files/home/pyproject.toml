[project]
name = "default"
version = "2024.11.17"
requires-python = ">=3.13"
dependencies = [
  "ansible-lint",
  "ansible",
  "ara[server]",
  "github3.py",
  "gnureadline",
  "mitogen",
  "pip",
  "ruff",
  "setuptools",
  "wheel",
]

[tool.uv]
environments = ["platform_system != 'Windows'"]
