---
- name: Developer
  hosts: localhost
  tasks:
    - name: Including roles
      ansible.builtin.include_role:
        name: "{{ role_item }}"
      loop:
        - languages
      loop_control:
        loop_var: role_item
